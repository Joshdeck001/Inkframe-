export const copilotCss = "#copilot-launcher { position:fixed; bottom:24px; right:24px; width:58px; height:58px; border-radius:50%;\n    background:linear-gradient(135deg,var(--blueGlow),var(--red)); border:none; cursor:pointer; z-index:200;\n    display:flex; align-items:center; justify-content:center; font-size:24px;\n    box-shadow:0 14px 34px -10px rgba(47,111,237,.6); }\n  #copilot-panel { position:fixed; bottom:96px; right:24px; width:340px; max-width:calc(100vw - 32px);\n    background:var(--panel); border:1px solid var(--border); border-radius:16px; padding:18px; z-index:200;\n    display:none; box-shadow:0 24px 60px -20px rgba(0,0,0,.6); }\n  #copilot-panel.open { display:block; }\n  .cp-header { display:flex; justify-content:space-between; align-items:center; margin-bottom:12px; }\n  .cp-title { font-weight:700; font-size:14px; }\n  .cp-status { font-size:11.5px; padding:4px 10px; border-radius:20px; font-weight:600; }\n  .cp-status.listening { background:rgba(76,139,255,.15); color:var(--blueGlow); }\n  .cp-status.muted { background:rgba(255,255,255,.06); color:var(--muted); }\n  .cp-status.thinking { background:rgba(255,180,50,.15); color:#ffc266; }\n  .cp-status.speaking { background:rgba(40,200,140,.15); color:#5fe3b8; }\n  .cp-transcript { max-height:180px; overflow-y:auto; margin-bottom:12px; font-size:12.5px; line-height:1.6; }\n  .cp-msg { margin-bottom:8px; }\n  .cp-msg .who { font-weight:700; font-size:11px; color:var(--muted); margin-bottom:2px; }\n  .cp-controls { display:flex; gap:8px; margin-bottom:10px; }\n  .cp-btn { flex:1; padding:10px; border-radius:10px; border:1px solid var(--border); background:var(--panel2);\n    color:var(--ink); font-size:12px; font-weight:600; cursor:pointer; text-align:center; }\n  .cp-btn.talk { background:linear-gradient(135deg,var(--blueGlow),var(--blue)); border:none; color:#fff; }\n  .cp-btn.muted-active { background:rgba(226,59,76,.15); border-color:var(--red); color:var(--redGlow); }\n  .cp-sub-controls { display:flex; gap:8px; font-size:11px; }\n  .cp-sub-btn { flex:1; padding:7px; border-radius:8px; border:1px solid var(--border); background:transparent;\n    color:var(--muted); cursor:pointer; text-align:center; }\n  .cp-sub-btn.paused { border-color:#ffc266; color:#ffc266; }";
